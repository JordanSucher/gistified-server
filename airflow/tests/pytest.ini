[pytest]
testpaths = tests
python_files = test_*.py
addopts = --cov=dags --cov-report=term-missing
markers =
    unit: mark a test as a unit test.
    integration: mark a test as an integration test.
    dags: mark a test as a DAG test.